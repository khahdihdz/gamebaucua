{
  "name": "baucua-casino",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "yarn workspace @baucua/api dev",
    "dev:web": "yarn workspace @baucua/web dev",
    "dev:admin": "yarn workspace @baucua/admin dev",
    "build:api": "yarn workspace @baucua/api build",
    "build:web": "yarn workspace @baucua/web build",
    "build:admin": "yarn workspace @baucua/admin build"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
